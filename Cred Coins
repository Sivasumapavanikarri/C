#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int X, Y;
        scanf("%d %d", &X, &Y);

        int coins = X * Y;
        int bags = coins / 100;

        printf("%d\n", bags);
    }
    return 0;
}
